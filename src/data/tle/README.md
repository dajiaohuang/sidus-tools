# GP snapshots

Dated TLE copies used when CelesTrak™ is unreachable or has asked this
client to wait. They are not live elements. Not actual position tracking.
Sidus is not affiliated with CelesTrak™.

Last pull from gp.php (GROUP + FORMAT=TLE): 2026-08-25. One GET per group,
including `starlink.txt` (~1.8 MB). Do not poll from CI every two hours:
GitHub runner IPs are shared and CelesTrak™ blocks them.
