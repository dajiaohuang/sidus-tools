# UI review screenshots

This directory contains visual evidence referenced by pull requests. It is not served as part of the application UI.

- `laser-link-invalid-panels.png`: local browser capture with transmitter efficiency `ηt = 1.2`; the result is rejected and the code-export panel is hidden.
